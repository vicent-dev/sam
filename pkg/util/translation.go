package util

// at the moment translations are not a thing in SAM but for now I decided to centralize the texts in a single package to make it easier to refactor/change
const ID_REQUIRED_ERROR_MESSGE string = "id is a required field"
const ID_TYPE_ERROR_MESSGE string = "id has to be a number"

const LOGIN_REQUIRED_FIELDS_ERROR_MESSAGE string = "username and password are a required fields"
const REFRESH_TOKEN_REQUIRED_FIELDS_ERROR_MESSAGE string = "refresh_token is a required field"
